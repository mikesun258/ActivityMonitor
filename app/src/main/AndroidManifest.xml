<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 模块主应用声明，使用独立进程隔离，避免和其他模块/宿主进程冲突 -->
    <application
        android:allowBackup="true"
        android:label="ActivityMonitor"
        android:supportsRtl="true"
        android:process=":xposed_module">
        
        <!-- Xposed 模块核心配置 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposedname"
            android:value="ActivityMonitor" />
        <meta-data
            android:name="xposeddescription"
            android:value="监听APP Activity启停，发送对应广播" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <!-- （可选）如果你的模块有主界面Activity，需要在这里声明 -->
        <!-- <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->
    </application>
</manifest>
